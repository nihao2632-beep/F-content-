# 打包工具（在 Windows 构建机上执行 pip install -r requirements.txt）
pyinstaller>=6.10

# 运行时依赖
pypdf>=4.0                # PDF 文字层提取（可选用；缺失时自动退回二进制扫描）
pypdfium2>=5.0            # PDF 页面渲染（扫描版 OCR 用）
rapidocr-onnxruntime>=1.3 # 离线中英文 OCR（扫描版 PDF 识别用）

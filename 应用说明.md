# 创可贴制作-内容搜索打开工具 使用说明

版本：V1.01 ｜ 适用系统：Windows 10 / Windows 11（64 位）

---

## 一、这个软件是做什么的？

一句话：**输入一段文字 → 选择一个文件夹 → 自动找出该文件夹下所有“内容包含这段文字”的文件，并用系统默认程序把命中文件全部打开。**

例如：你有一堆 Word 文档，想找出所有**内容里**提到「党委会」的文件并一起打开核对，
只需输入 `党委会`、选择文档所在文件夹、点一下按钮，剩下的交给软件。

> 它查找的是**文件正文内容**，而不是文件名。
> 例如文件名写着“现教处”，但正文里写的是“现代教育技术处”，搜索“现教处”不会命中；
> 如果确实想按文件名找，勾选“包含文件名”即可。

---

## 二、安装与启动

- 本软件是**绿色单文件**程序：把 `创可贴制作-内容搜索打开工具.exe` 拷贝到任意 Windows 电脑即可使用，**无需安装、无需 Python**。
- 双击 exe 启动。
- 首次运行时，Windows 若出现蓝色“Windows 已保护你的电脑”提示：
  点击 **更多信息 → 仍要运行** 即可（单文件小工具常见提示，软件本身完全本地运行、无网络行为）。
- 建议：把 exe 放到一个固定文件夹（例如 `D:\工具\`），再右键 → 发送到 → 桌面快捷方式，方便日常使用。

---

## 三、快速上手（三步完成）

1. **输入要查找的内容**：在“输入要查找的内容”框里输入文字（支持多关键词，见第六节）。
2. **选择文件夹**：点“浏览…”按钮，选择要搜索的文件夹（默认包含所有子文件夹）。
3. **点“搜索并打开全部”**：软件搜索完成后会弹窗提示共找到多少个文件，点“是”，
   就会用系统默认程序（Word / WPS / Excel / PDF 阅读器等）把所有命中文件**全部打开**。

示例：在文件夹里搜索 `党委会`，命中 2 个 docx → 软件询问 → 确定 → 2 份文档一起打开。

---

## 四、界面功能详解

### ① 输入要查找的内容
- 输入任意文字（中文/英文/数字/符号均可）。
- **每行算一个关键词**：写了多行时，只要文件命中其中任意一行就会被找出（见第六节）。

### ② 选择要搜索的文件夹
- “浏览…”：打开系统对话框选择文件夹。
- “用上次目录”：一键回到上次使用过的文件夹（软件会自动记住）。

### ③ 搜索选项
| 选项 | 说明 |
| --- | --- |
| 包含子文件夹 | 勾选后把所选文件夹下所有子文件夹一起搜索（默认勾选） |
| 忽略大小写（英文） | 搜英文时忽略 A/a 区别（默认勾选） |
| 包含文件名 | 文件名里包含关键词也算找到（默认不勾选） |
| 包含隐藏文件/目录 | 是否搜索隐藏文件（默认不勾选） |
| 扫描版PDF用OCR识别 | 对没有文字层的扫描版/图片 PDF 进行 OCR 识别后再搜索（默认勾选，较慢；结果会缓存，再次搜索很快） |
| 只搜索类型 | 例如填 `.docx,.pdf,.txt` 只搜这几种；留空=全部 |
| 单个文件上限(MB) | 大于该大小的文件跳过，加快速度；0=不限（默认 200） |

### ④ 操作按钮
| 按钮 | 作用 |
| --- | --- |
| 🔍 搜索并打开全部 | 搜索，确认后自动打开全部命中文件 |
| 仅搜索（不打开） | 只列出结果，不打开文件 |
| 停止 | 文件夹很大时，可随时停止本次搜索 |
| 打开选中的文件 | 只打开结果列表中选中的文件（可按住 Ctrl 多选） |
| 复制选中路径 | 把选中文件的完整路径复制到剪贴板 |
| 清空结果 | 清空下方结果列表 |

- 结果列表：双击某一行 = 打开该文件；底部状态栏会实时显示“已扫描 N 个文件，命中 M 个”。

---

## 五、支持搜索的文件格式

| 类型 | 说明 |
| --- | --- |
| 文本类 | txt、md、csv、log、json、xml、html、源代码等（自动识别 UTF-8、GBK、UTF-16、Big5） |
| Word | `.docx`（含页眉页脚）；旧版 `.doc` 按文件字节查找 |
| Excel / PPT | `.xlsx`、`.pptx` |
| OpenDocument | `.odt`、`.ods`、`.odp` |
| PDF | `.pdf`（内置文字提取；扫描版图片 PDF 因无文字层无法搜索） |
| 其它任意文件 | 直接查找文件字节（支持 UTF-8 / GB18030 / UTF-16 中文编码） |

搜索过程**只读**，绝不会修改或删除你的任何文件。

---

## 六、多关键词怎么用？

在输入框里**每行写一个关键词**，文件只要**命中其中任意一行**就会被找到（空行忽略）。

示例：
```
党委会
6万元
```
→ 会找出正文里含“党委会”**或**含“6万元”的所有文件。

如果只想精确搜一整段话，就把它写成**一行**（不要换行）。

---

## 七、常见问题（FAQ）

**1. 为什么搜某个词一个文件都找不到？**
先确认：① 该文件夹里确实有这些文件；② 词是否在**正文**里（不是只在文件名里，可勾选“包含文件名”）；③ 是否被“只搜索类型/单个文件上限”限制了。

**2. 搜到很多不相关的文件？**
把关键词写得更具体（如 `安装网络申请` 代替 `网络`），或在“只搜索类型”里限定 `.docx,.pdf`。

**3. 打开失败 / 打不开？**
说明这台电脑没安装能打开该格式的程序（例如没装 Word/WPS 就打不开 docx）。文件本身不会被损坏。

**4. 文件夹很大、搜索很慢？**
在“单个文件上限(MB)”里填小一点（如 50）跳过视频/压缩包等大文件，或缩小搜索范围，必要时点“停止”。

**5. PDF 搜不到内容？**
扫描版/图片型 PDF 没有文字层，无法按文字搜索；文字版 PDF 可以搜到。

**7. 双击 exe 没反应或被拦截？**
见第二节：点击“更多信息 → 仍要运行”；若被杀毒软件误报，请将该 exe 加入信任区（PyInstaller 打包的绿色单文件常有此误报）。

**7. 软件会上传我的文件吗？**
不会。程序**完全本地运行、不联网**，搜索结果和配置（仅记住上次文件夹与关键词）只保存在本机。

---

## 八、版本记录

| 版本 | 更新内容 |
| --- | --- |
| 1.0（2026-09-05） | 首个发布版：内容搜索+一键打开、支持多关键词、创可贴主题图标 |
| 1.01（2026-09-06） | 新增：扫描版/图片 PDF 离线 OCR 搜索；版本号 1.01（与 V1.0 分开存放、不覆盖） |

---

*本说明随软件一同提供。使用中如有问题，请联系软件提供方。*
